pipeline {
 agent any
     stages { 
       stage ('First') { 
        steps {
                 sh ''' echo "$BUILD_NUM" '''
      }
       }  
        stage ('Second') {
         steps {
          echo ''' echo "hi" '''
          }
        } 
   }
   }
